# High-resolution sprite masters

These are the original **1024×1024** generated sprites.

The files next to `assets/sprites/` (256×256) are what the prototype actually draws. Godot ignores this folder (`.gdignore`) so the masters are not imported as game textures.

To raise in-game resolution, resize from here and replace the matching file in `assets/sprites/`. Keep the same filenames.
